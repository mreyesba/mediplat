# Copy this file to `.env` and fill in real values. `.env` is gitignored —
# never commit it. Each developer/environment generates their own SECRET_KEY;
# it does not need to match anyone else's.

# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=

# Not read yet — placeholder for the upcoming Postgres migration.
# DATABASE_URL=
